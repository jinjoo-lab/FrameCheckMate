server:
  port: 8761

eureka:
  server:
    enable-self-preservation: false
  instance:
    hostname: k11a607.p.ssafy.io
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
